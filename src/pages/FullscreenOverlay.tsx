import React from "react";
import { Button } from "../components/Button";
const { ipcRenderer } = require("electron");

export const FullscreenOverlay = () => {
    return (
        <>
            <h1>FULL SCREEN OVERLAY</h1>
        </>
    );
};
